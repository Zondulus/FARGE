GROUNDEFFECTFAR
{
    maxDragReduction = 0.5
    wingspanScale = 1.0     // Multiplier for wingspan detection (1.0 = 100%)
    defaultWingspan = 10.0  // Fallback if vessel size cannot be calculated
    debugMode = false
}